#!/bin/bash
# Database Migration Script - Tambah Categories

echo "=========================================="
echo "To-Do List Database Migration"
echo "=========================================="
echo ""
echo "LANGKAH-LANGKAH MIGRASI:"
echo "1. Buka phpMyAdmin (http://localhost/phpmyadmin)"
echo "2. Login dengan akun MySQL Anda"
echo "3. Pilih database 'todo_list'"
echo "4. Klik tab 'SQL'"
echo "5. Copy dan paste kode berikut:"
echo ""
echo "=========================================="
echo "KODE SQL UNTUK DIJALANKAN:"
echo "=========================================="
echo ""
echo "-- Tambah table categories"
echo "CREATE TABLE IF NOT EXISTS categories ("
echo "    id INT PRIMARY KEY AUTO_INCREMENT,"
echo "    user_id INT NOT NULL,"
echo "    name VARCHAR(255) NOT NULL,"
echo "    emoji VARCHAR(10) DEFAULT '📝',"
echo "    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,"
echo "    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,"
echo "    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,"
echo "    UNIQUE KEY unique_category_per_user (user_id, name)"
echo ");"
echo ""
echo "-- Modify todos table untuk menambah category_id"
echo "ALTER TABLE todos ADD COLUMN category_id INT AFTER user_id;"
echo "ALTER TABLE todos ADD FOREIGN KEY (category_id) REFERENCES categories(id) ON DELETE CASCADE;"
echo ""
echo "-- Tambah index untuk performa"
echo "CREATE INDEX idx_user_categories ON categories(user_id);"
echo "CREATE INDEX idx_category_todos ON todos(category_id);"
echo ""
echo "=========================================="
echo "Atau jalankan file: db_setup_updated.sql"
echo "=========================================="
echo ""
echo "Setelah migrasi selesai, fitur dinamis kategori akan langsung berfungsi!"
